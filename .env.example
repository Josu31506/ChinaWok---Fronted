# ===========================================
# URLs de Microservicios AWS Lambda + API Gateway
# ===========================================
# Cuando tengas las APIs desplegadas, reemplaza estas URLs
# con las URLs reales de tus API Gateways

# Microservicio de Usuarios (CRUD Usuarios, Login, Registro)
VITE_API_USERS_URL=https://tu-api-usuarios.execute-api.region.amazonaws.com/prod

# Microservicio de Locales (CRUD Locales, Tipos de despacho)
VITE_API_STORES_URL=https://tu-api-locales.execute-api.region.amazonaws.com/prod

# Microservicio de Pedidos (incluye Productos, Pedidos, Ofertas, Combos)
VITE_API_ORDERS_URL=https://tu-api-pedidos.execute-api.region.amazonaws.com/prod

# Timeout para requests HTTP (en milisegundos)
VITE_API_TIMEOUT=10000
